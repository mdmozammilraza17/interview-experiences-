# Yeppar Smart Solution Interview Questions

- **Company Name:** Yeppar Smart Solution, Jaipur
- **Role:** Java Developer (3 Years Experience)
- **Date of Interview:** 22/06/2026
- **Time:** 3:30 PM - 4:30 PM
- **Mode of Interview:** Virtual

---

## Interview Questions

1. Tell me about yourself and what projects you have worked on.
2. What are your roles and responsibilities in your current project?
3. Explain the end-to-end lifecycle of a request in your microservices-based project.
4. What is the purpose of an API Gateway in a microservices architecture?
5. What is Spring Cloud, and which Spring Cloud components have you used?
6. Do you have experience with Java 17 and Java 21? Which features have you used?
7. What are the major features introduced in Java 8?
8. What is the Stream API, and what is it used for?
9. Is Java a pure object-oriented language? Why or why not?
10. Can Java be made to behave like a pure object-oriented language? How?
11. What is the difference between `int` and `Integer`?
12. Can constructors be overridden? Why or why not?
13. What would happen if Java allowed constructors to be overridden?
14. What is polymorphism?
15. What are the different types of polymorphism in Java?
16. What is the difference between the following two declarations?

```java
public final class Employee {
}
```

```java
class Employee {
    final void calculateSalary() {
    }
}
```

17. What will be the output of the following code?

```java
String s1 = "Java";
String s2 = "Java";

String s3 = new String("Java");
String s4 = s3.intern();

System.out.println(s1 == s2);
System.out.println(s1 == s3);
System.out.println(s1 == s4);
System.out.println(s3 == s4);
```

18. Explain the memory allocation and String Pool behavior for the above code.
19. What is the `static` keyword in Java?
20. What are the uses of static variables, methods, and blocks?
21. What is the difference between `HashMap` and `ConcurrentHashMap`?
22. How does `ConcurrentHashMap` achieve thread safety?
23. How does the `synchronized` keyword work in Java?
24. What is the difference between Spring Framework and Spring Boot?
25. Which Spring Boot dependencies have you used in your project?
26. What is Dependency Injection (DI)?
27. What are the different types of Dependency Injection?
28. Which type of Dependency Injection do you prefer and why?
29. What is the difference between `@Component` and `@ComponentScan`?
30. Write a SQL query to fetch 50 records after skipping the first 5000 records from a table. Explain your approach.
31. Write a SQL query to find employees who earn more than their managers.
32. Write a Java program to count the frequency of words in the following input using a brute-force approach.

Input:

```java
{"apple", "orange", "mango", "apple", "orange"}
```

Expected Output:

```java
{"apple":2, "orange":2, "mango":1}
```

33. Write a Java program to find the second highest number in the following array.

```java
[2, 5, 8, 1, 6, 9]
```
